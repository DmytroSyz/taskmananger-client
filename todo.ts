export interface Todo {
  id: string;
  title: string;
  description: string;
  startDate: string;
  finishDate: string;
  priority: string;
  status: boolean;
  isChecked: boolean;
  userId: string;
}
